1) Написать функцию, которая умножает заданное число на 2 (суммирует его с смаим собой);
2) Написать функцию, которая складывает 2 заданных числа и умножает на 2 (порядок действий в данном случае, не важен);
3) Написать функцию, которая определяет максимальный из двух заданных на вход элементов (не называть её "max". т. к. это системная функция);
4) Написать функцию, которая определяет максиумм из трёх заданных на вход элементов (Саня также предложил написсть её, используя раннюю функцию, определящую максимум из двух элементов);
5) Написать функцию-словарь колхоз, которая выдает "One", если на входе число 1, "Two", если на входе число 2, "No One no Two", если на входе другое число;
6) Дополнить функцию-словарь колхоз, которая будет выдавть соответствующую строку при написании на вход числа и число при написании на вход строки (это домашнее заданее, использовать только одну функцию);
7) Написать функцию, сумммирующую все элементы заданного на вход списка;
8) Написать функцию, которая помещает заданный элемент в конец заданного списка (аналог функции push-back у контейнера vector в С++);
9) Написать функцию, которая помещает 1-ый элемент заданного списка в конец (такми оборазом, чтобы 1-ый был исключен из начала);
10) Написать функцию, которая помещает последний элемент заданного списка в начало (у нас получилось с помощью трёх функций, т. к. Терновский сказал, что данную задачу сделать возможно с помощью одной функции, используя лямбда-выражения, которые мы пока не проходили, при этом, как и в предыдущей задаче, последний элемент должен быть исключен из конца списка);
11) Написать функцию, которая возвращает 2-ой элемент из заданного на вход списка;
12) Написать функцию, которая возвращает предпоследний элемент из заданного на вход списка (Некит сделал сначала с помощью трех функций, потом с помощью одной, вроде бы без лямбда-выражений);
13) Написать функцию, которая возвращает элемент из заданного на вход списка по заданному на вход индексу (это домашнее задание)
-- На сегодня всё :-)
